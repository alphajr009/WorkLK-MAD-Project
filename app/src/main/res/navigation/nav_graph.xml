<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/mobile_navigation"
    app:startDestination="@id/About">

    <fragment
        android:id="@+id/About"
        android:name="com.example.worklk_madproject.About"
        android:label="about"
        tools:layout="@layout/fragment_about">
        <action
            android:id="@+id/action_About_to_EditAbout"
            app:destination="@id/EditAbout" />
    </fragment>

    <fragment
        android:id="@+id/EditAbout"
        android:name="com.example.worklk_madproject.EditAbout"
        android:label="edit_about"
        tools:layout="@layout/edit_about" />

    <!-- Add other fragments here -->
</navigation>
